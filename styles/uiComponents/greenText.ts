import { StyleSheet } from "react-native";

const textStyles = StyleSheet.create( {
  base: {
    letterSpacing: 0,
  },
  center: {
    textAlign: "center",
  },
  defaultSize: {
    fontSize: 18,
    lineHeight: 24,
  },
  small: {
    fontSize: 16,
    lineHeight: 22,
  },
  smaller: {
    fontSize: 15,
    lineHeight: 20,
  },
} );

export default textStyles;
