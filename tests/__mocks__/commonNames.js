// Mock for the gitignored, generated utility/commonNames directory
// (built from commonNames.tar.gz — see README). Tests don't need real
// common name dictionaries, just the module shape.
export default {
  commonNames0: [],
};
